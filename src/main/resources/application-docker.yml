spring:
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0

  datasource:
    url: jdbc:mysql://mysql:3306/db_takeout?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
  data:
    redis:
      host: redis
      port: 6379
