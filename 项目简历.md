TakeoutSystem 外卖订单系统全栈开发                                    2026.5~2026.8 
 	 l 	 技术栈： Java、Spring Boot、MyBatis-Plus、MySQL、Redis、Vue3、Vite、Vant、Element Plus、Docker、GitHub Actions 
 	   	 项目概述：基于 Spring Boot 3 单体架构搭建外卖平台，打通商家入驻、菜品管理、购物车、下单支付、订单状态流转、评价收藏全业务链路，服务消费者、商家、管理员三类角色，实现复杂订单状态机的安全流转与高并发库存一致性保障。 
  高并发订单与库存安全 ：自实现 Snowflake 全局 ID 生成器，保证订单号、支付号全局唯一且不暴露业务量；基于 Redis Lua 脚本实现多菜品「校验库存 + 扣减」原子操作，彻底杜绝并发超卖；订单状态流转采用 Redis 分布式锁（setIfAbsent）+ 乐观锁（WHERE status = expected）双重保障，确保「待支付→待接单→备餐中→配送中→已完成」单向流转，防止重复接单与重复支付，取消订单时库存自动回滚。 
 l  认证鉴权与用户上下文 ：基于 JWT（JJWT）+ 手机号短信验证码实现登录，设计 AuthInterceptor 统一拦截校验 Bearer Token，解析后通过 ThreadLocal UserContext 透传用户身份；afterCompletion 强制清理 UserContext 防止线程池复用串号，精准区分白名单接口与回调场景，解决支付回调 UserContext 为空导致的 401 与登录跳转问题。 
 l  前端精度与工程规范 ：JacksonConfig 统一将 Long 序列化为 String，避免前端 JS Number 对 Snowflake ID 的精度丢失（>2^53）；全接口采用 Result<T>{code, message, data} 统一响应包装，GlobalExceptionHandler 全局捕获 BusinessException，ResultCode 统一错误码，Controller→Service→Mapper 分层架构，包名与文件夹路径严格对应。 
 l  三端前端工程化 ：采用 Vue3 + Vite 开发消费者 H5（Vant4 移动端）、商家端与管理后台（Element Plus），基于 Pinia 状态管理；商家端集成 WebSocket 实现订单实时推送；三端共用 axios 封装请求层，统一处理 401 自动跳登录与 Token 刷新，通过角色权限隔离不同端功能可见性。 
 l  DevOps 与自动化交付 ：采用 Docker Compose 编排 MySQL、Redis、后端、三端 Nginx 共 6 个服务，实现 docker compose up -d 一键启动；基于 GitHub Actions 监听 main 分支推送，4 个 Job 并行构建并推送镜像至 Docker Hub（latest + commit SHA 双标签支持回滚），代码提交后镜像自动构建发布；配套 PowerShell 运维脚本实现一键启停、诊断、冒烟测试全流程自动化。 
